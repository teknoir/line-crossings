apiVersion: v1
kind: Service
metadata:
  name: line-crossings
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: line-crossings
  ports:
    - name: http
      port: 80
      targetPort: 3000
